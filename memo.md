# git 처음 사용시 
git config --global --list # 리스트 확인해야함 없으면 밑에 작업 실행
# 이름 설정
git config --global user.name yy9976com

# 이메일 설정
git config --global user.email "yy9976com@gmail.com"


git remote add origin git@github.com:parkyhun/argocd_test.git